# Copy to `.env` for local runs and Docker Compose (`env_file: .env`).

DATABASE_URL=postgresql://checkout:checkout@postgres:5432/checkout
SECRET_KEY=replace-with-a-long-random-string-for-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
